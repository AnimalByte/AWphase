# Phase7F augmented XGBoost experimental

Experimental augmented-feature learned gate.

Adds:
- log-transformed panel features
- panel evidence normalized by haplotypes
- anchor distance features
- two-sided anchor features
- Phase6C block size/span/density features

Interpretation:
Phase7F proves that anchor/block geometry carries useful signal, but it is not yet a strict improvement over v4b. Keep as experimental until more windows are added.
