# Status Notes

Files in this directory are historical phase notes unless they explicitly say
otherwise. Use `docs/current_default.md` as the canonical current repository
pointer.

Older documents may describe the default at the time they were written. Those
statements are not current if they conflict with the Phase9D default.
