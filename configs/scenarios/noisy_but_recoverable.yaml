sample_id: HG002_noisy
chromosome: chr20
region: chr20:1-5000000

inputs:
  target_vcf: data/scenarios/noisy_but_recoverable/hg002_chr20.vcf.gz
  target_reads: data/scenarios/noisy_but_recoverable/hg002_chr20_reads.json
  target_variants: data/scenarios/noisy_but_recoverable/hg002_chr20_variants.json
  target_site_priors: data/scenarios/noisy_but_recoverable/hg002_chr20_site_priors_ancestry_on.json
  target_block_summaries: null
  panel_path: data/scenarios/noisy_but_recoverable/panel_chr20.bin
  truth_vcf: data/scenarios/noisy_but_recoverable/hg002_chr20_truth.vcf.gz

ancestry:
  groups: [afr, amr, eas, eur, sas, mid, oce]
  weights: [0.02, 0.03, 0.02, 0.88, 0.02, 0.02, 0.01]

candidate_engine:
  top_k_donors: 32
  max_composite_states: 16

decoder:
  window_size: 50000
  overlap_size: 5000
  lambda_panel: 1.0
  lambda_anc: 0.5
  lambda_switch: 0.2

bridge:
  min_informative_sites: 4
  min_confidence: 0.6
  max_gap_bp: 100000
